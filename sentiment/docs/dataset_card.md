# Dataset Card: Sentiment Analysis

## Назва проєкту
Аналіз тональності українських відгуків про товари (Ukrainian User Review Sentiment Analysis).

## Задача
**Задача A: Класифікація текстів (тональність).**
* **Input:** Текст відгуку користувача.
* **Output:** Мітка тональності: `positive` або `negative`.
* **Додаткова задача (IE):** Витяг структурованої інформації (дати, суми, локації) з неструктурованого тексту відгуків за допомогою правил та словників.

## Джерело даних
[Ukrainian User Review Sentiment Analysis Dataset (Kaggle)](https://www.kaggle.com/datasets/alexsukhrin/ukrainian-user-review-sentiment-analysis-dataset).
Датасет містить відгуки з українських маркетплейсів.

## Обсяг
* **Кількість текстів:** 3034.
* **Кількість класів:** 2 (`negative`: 1922, `positive`: 1112).
* **Середня довжина:** ~164 слова / ~1097 символів.

## Мова та Домени
* **Мова:** Українська (UA), з наявністю суржику та російськомовних вкраплень.
* **Домен:** Відгуки про побутову техніку, електроніку та інші товари.

## Очищення (v2)
Виконано глибоку обробку тексту за допомогою кастомного пайплайну (`TextPreprocessor`):
1. **Базове очищення:** Декодування HTML-сутностей та видалення технічного сміття (фрази "розгорнути", "читати далі", "відповідь" тощо).
2. **Нормалізація символів:** Заміна латинських гомогліфів на кириличні аналоги, уніфікація всіх типів апострофів до стандартного `'`.
3. **Обробка регістру та пунктуації:** Зниження регістру для слів, написаних Caps Lock. Стиснення надлишкової пунктуації (`!!!` -> `!!`, `.....` -> `...`) та виправлення пробілів перед розділовими знаками.
4. **Форматування інтервалів:** Додавання пробілів після адресних скорочень (напр., `м. Київ`) та відділення чисел від одиниць виміру/валют.
5. **Маскування PII (Конфіденційних даних):** Усі чутливі дані та ідентифікатори замінено на безпечні токени: `<EMAIL>`, `<URL>`, `<PHONE>`, `<ID>`.
6. **Sentence Splitting:** Текст розбито на окремі речення з урахуванням захисту від хибних розривів на відомих українських скороченнях та десяткових числах.

## Лінгвістичні ознаки (v3) та Lemma/POS Decision
За допомогою бібліотеки **Stanza** (модель `uk`) датасет було збагачено лінгвістичними ознаками:
* Згенеровано `lemma_text` (текст, зведений до початкових форм).
* Згенеровано `pos_seq` (послідовність частин мови, напр., `NOUN VERB ADJ`).

**Фінальне рішення щодо Lemma/POS (Lemma/POS Decision):**
* **Використовуємо леми:** У ході експериментів доведено, що використання `lemma_text` покращує точність класифікатора (Accuracy зросла на +2.80% при обмеженому словнику TF-IDF). Лематизація ефективно зменшує розрідженість даних, дозволяючи моделі фокусуватися на сенсі емоційних слів.
* **POS-теги відкидаємо:** Використання POS-тегів та граматичних біграмів для генерації ознак призвело до падіння метрик (-2.64%), оскільки вони створюють інформаційний шум і витісняють корисні слова з лімітованого словника.

## Інформаційний витяг (Information Extraction - v4)
Реалізовано rule-based модуль (Regex + словники) з підходом Precision-first для витягу 3 типів полів:
1. **DATE (Дати):** Витяг календарних дат. Нормалізація до формату `YYYY-MM-DD`.
2. **AMOUNT (Грошові суми):** Витяг сум із прив'язкою до валюти. Нормалізація до `float ISO_CURRENCY` (напр., `499.0 UAH`).
3. **LOCATION (Міста):** Витяг назв українських міст за допомогою словника відмінків. Нормалізація до Називного відмінку (напр., `у Львові` -> `Львів`).

## Ризики та Конфіденційність (PII)
1. **Дисбаланс класів:** Негативних відгуків значно більше, ніж позитивних (1922 vs 1112). Модель може бути упередженою в бік негативного класу.
2. **Ризики PII та деанонімізації:** Прямі ідентифікатори (телефони, email, номери замовлень) надійно сховані за масками (`<PHONE>`, `<EMAIL>`, `<ID>`). Проте витяг полів `LOCATION` (місто проживання/доставки) та `DATE` (точна дата транзакції) створює непрямий ризик деанонімізації (т.зв. "мозаїчний ефект"), якщо ці дані будуть зіставлені зі зламаними базами даних інтернет-магазинів.
3. **Втрата контексту через маскування:** Токен `<ID>` іноді може замаскувати важливі для тональності числові показники (наприклад, оцінки).
4. **Обмеження NLP-інструментів:** * Stanza погано розпізнає повністю російськомовні відгуки, англійські назви брендів та суржик.
    * Rule-based екстрактор є чутливим до орфографічних помилок (наприклад, неправильно написані назви міст не будуть витягнуті, оскільки не збігатимуться зі словником).

## Splits & leakage
* **Стратегія розділення:** Застосовано Stratified random split у пропорції 80/10/10 (Train/Val/Test). Це дозволило ідеально зберегти вихідний баланс класів (~63% `negative` та ~37% `positive`) у всіх підвибірках, запобігаючи спотворенню розподілу (Target distribution skew).
* **Витоки через дублікати (Duplicate Leakage):** Під час перехресної перевірки виявлено незначний витік (2 точні дублікати, що перетиналися між тренувальною та тестовою/валідаційною вибірками). Ці записи підлягають видаленню з `train` для забезпечення чесної оцінки метрик.
* **Шаблонні підказки (Template Leakage):** Технічних підказок (метаданих) у текстах не виявлено. Регулярні вирази зафіксували наявність слів "позитив" та "негатив", проте ручний аналіз підтвердив, що це природна лексика авторів відгуків, яка не є витоком.
* **Приховані ризики (Group/Time Leakage):** Через відсутність у датасеті ідентифікаторів користувачів (user_id) та міток часу (date) неможливо гарантувати відсутність перетину авторів між сплітами, а також відслідкувати зміну лексики у часі (Data Drift).

## Classification Baseline (Оновлено після ЛР6)
Для встановлення точки відліку було побудовано baseline-класифікатор:
* **Модель:** Logistic Regression.
* **Використані фічі:** Експерименти проводилися на текстах `processed_v2` та `lemma_text` з використанням TF-IDF векторизації (word n-grams (1,2)).
* **Результат:** Найвищу точність показав варіант на `lemma_text` із застосуванням `class_weight='balanced'`, що значно підвищило Macro-F1 за рахунок кращого розпізнавання негативного класу.

## Доповнення за результатами Лабораторної роботи №7 (Linear SVM & Thresholds)
* **Чи є imbalance:** Так, у датасеті наявний стабільний дисбаланс класів: близько 63% відгуків належать до класу `negative`, і лише 37% — до `positive`.
* **Чи noisy text / translit / spelling variation є важливою проблемою:** Хоча в текстах зустрічаються друкарські помилки та суржик, вони не є головною перешкодою. Значно більшою проблемою для лінійних моделей виявився семантичний шум: overlap класів (коли в одному відгуку є і плюси, і мінуси) та noisy labels (хибна розмітка відгуку користувачем).
* **Чи char-ngrams допомогли:** Ні, використання символьних n-грамів (`char_wb`, 3-5) лише погіршило результати порівняно з цілими словами (Macro-F1 впав з 0.8262 до 0.8093). Це підтверджує, що для визначення тональності в цьому датасеті смислове навантаження цілих слів (лем) значно важливіше за їх морфологічні чи орфографічні варіації.
* **Який клас був найпроблемніший:** Найпроблемнішим виявився клас `positive`. Через дисбаланс датасету та часте використання сарказму або заперечень (напр., "не є підробкою"), моделі було важко його ідентифікувати, що призводило до найнижчих показників повноти (Recall).
* **Який компроміс precision / recall виявився доречним:** Завдяки використанню `class_weight="balanced"`, стандартний поріг рішення (`0.000` для SVM) виявився оптимальним для максимізації загального F1-score. Проте, для бізнес-сценаріїв (наприклад, максимальне охоплення клієнтів для нарахування бонусів за хороший відгук) доречним виявився компроміс **Recall-first**: зниження порогу до `-0.500` дозволило підняти Recall позитивного класу з 0.70 до 0.92, пожертвувавши Precision (впав до 0.61).

## Головні ризики, що лишилися
За результатами Error Analysis базової моделі виявлено такі ризики:
* **Дисбаланс:** Частково компенсований балансуванням ваг, але природний перекіс даних все ще призводить до хибного класифікування деяких позитивних відгуків як негативних.
* **Overlap класів:** Лінійна модель на базі TF-IDF погано справляється з текстами змішаної тональності (наприклад, коли відгук починається з позитиву, але закінчується скаргою після слова "але").
* **Noisy labels:** Велика кількість гнівних відгуків (скарги на сервіс) мають хибну мітку `positive` у "золотому стандарті" від користувачів, що штучно занижує метрики.
* **Drift / Leakage:** Модель схильна запам'ятовувати назви конкретних магазинів (напр., слово "цитрус" у топі позитивних ознак) замість вивчення загальної семантики, що може призвести до падіння точності на нових даних.
* **Translit / Slang:** Суржик та нестандартне написання слів продовжують бути викликом для векторизатора.

## План наступного кроку
1. **Боротьба з дисбалансом:** (Виконано у ЛР6 через `class_weight='balanced'`).
2. **Фільтрація датасету:** Провести чистку від шумної розмітки (noisy labels) та додати назви брендів у стоп-слова для запобігання leakage.
3. **Експерименти з ембедингами та архітектурою:** Для вирішення проблеми "overlap класів" доцільно перейти від TF-IDF до Word2Vec, FastText або трансформерів (BERT), які краще вловлюють контекст.
4. **Інтеграція ознак IE:** Дослідити, чи впливає наявність згадок про ціну (`AMOUNT`) або локації (`LOCATION`) на тональність відгуку (наприклад, чи частіше скаржаться на проблеми з доставкою у певних містах).
5. **Тюнінг гіперпараметрів:** Використати `GridSearchCV` для пошуку оптимальних параметрів моделі Logistic Regression.